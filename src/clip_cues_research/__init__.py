"""clip-cues-research: revision experiments for ARRAY-D-26-00829.

This package lives alongside the published ``clip_cues`` package (same repo, revision branch)
and holds only the *new*, revision-specific experiment code: forensic baselines (E1) and
analysis utilities (E4/E5). Genuine library improvements (e.g. multi-backbone support for E3)
are made directly in ``clip_cues`` so they remain clean back-port candidates.

See PLAN.md for the experiment-to-reviewer mapping.
"""

__version__ = "0.1.0"
